export interface JwtUserData {
  id: string;
  username: string;
  role: string;
}

export interface User {
  address: string;
  email: string;
  full_name: string;
  id: string;
  phone_number: string;
  username: string;
}
